/*25) Faça um programa que leia e imprima uma matriz de 3x3 elementos inteiros. Em seguida,
calcule e imprima a sua transposta.*/

#include <stdio.h>
	int main(){
		int A[][]
		for (l = 0; l < 3; l++){
			for (c = 0; c < 2; c++){
				printf("Informe o valor de A[%d][%d]:\n\n", l, c);
				scanf("%d", &A[l][c]);
			}
		}
		for(l = 0; l < 3; l++){
				for(c = 0; c < 2; c++){
				printf("A[%d][%d]: %d\t", l, c, A[l][c]);
				
			}
		}
	
	}