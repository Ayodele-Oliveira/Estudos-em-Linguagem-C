 /*2) Escreva um programa que contenha uma variável do tipo inteiro chamada idade, duas
variáveis reais chamadas peso e altura. Atribua valores para cada uma e imprima os
seus valores.*/

#include <stdio.h>
	int main(){
		float altura = 1.55, peso = 52.0;
	
		printf("A altura é: %.2f\n", altura);
		printf("O peso é: %.2f\n", peso);

}